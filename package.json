{
    "scripts": {
        "frontend:install": "cd reactnd-contacts-complete && npm install && npm audit fix",
        "backend:install": "cd reactnd-contacts-server && npm install && npm audit fix",
        "frontend:build": "cd reactnd-contacts-complete && npm run build",
        "backend:build": "cd reactnd-contacts-server && npm run build"
    }
}